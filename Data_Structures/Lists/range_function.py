#Using the range() function: If you need to print index, then can use the range() function as shown in the code given
#below

num_list = [1,2,3,4,5]
for i in range(len(num_list)):
    print(f"Index : {i}")